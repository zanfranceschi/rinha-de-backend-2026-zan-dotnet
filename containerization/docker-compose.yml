x-api: &api
  image: zanfranceschi/rinha-de-backend-2026-zan-dotnet:latest
  platform: linux/amd64
  volumes:
    - sock:/run/sock
  deploy:
    resources:
      limits:
        cpus: "0.4"
        memory: "160M"

services:
  api1:
    <<: *api
    environment:
      SOCKET_PATH: /run/sock/api1.sock
    cpuset: "0,1"

  api2:
    <<: *api
    environment:
      SOCKET_PATH: /run/sock/api2.sock
    cpuset: "2,3"

  nginx:
    image: nginx:alpine
    platform: linux/amd64
    ports:
      - "9999:9999"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - sock:/run/sock
    depends_on:
      - api1
      - api2
    cpuset: "0,2"
    deploy:
      resources:
        limits:
          cpus: "0.2"
          memory: "30M"

volumes:
  sock:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: "size=4m,mode=0777"
